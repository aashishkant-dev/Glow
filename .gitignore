node_modules/
.env
.env.*
!.env.example
*.log
.DS_Store
nginx/certs/
mobile/.env
mobile/.env.*
mobile/node_modules/
.expo/
mobile/.expo/
.claude/
.vercel
uploads/
glow.env
apikey_youcam.txt

# AI tools
.gemini/
.idea/

# Dev docs / AI plans
docs/superpowers/
.superpowers/
CLAUDE.md
DEPLOYMENT_TODO.md
EXPANSION_PROMPT.md
GUIDE.md
IMPROVEMENTS.md
IMPROVEMENTS_PROMPT.md
LOCAL_DEV.md
landing.txt

# Misc junk
YES
YES.pub
# "screenshot/" (singular) never matched the actual folders below — they're
# "screenshots" and "screenshots new", so neither was ever actually ignored.
screenshot/
screenshots/
screenshots new/

# Build output (deployed via Vercel)
mobile/dist/

# Native (Expo bare) — build artifacts + local/secret files, NOT committed
mobile/android/build/
mobile/android/.gradle/
mobile/android/app/build/
mobile/android/local.properties
mobile/android/app/debug.keystore
mobile/android/app/release.keystore
mobile/android/*.keystore
mobile/ios/build/
mobile/ios/Pods/
mobile/ios/*.xcworkspace/xcuserdata/
mobile/ios/DerivedData/

# Docker / infra (not used)
Dockerfile
docker-compose.yml
.dockerignore
infra/
nginx/

# Dev-only scripts (legacy root files; scripts/seed-admin.js IS tracked)
/seed-admin.js
/seed-admin-remote.js
setup-portproxy.ps1
start-expo.sh

# Playwright screenshots
.playwright-mcp/

# (CI workflow is now at .github/workflows/ci.yml)
tests/

# Firebase / FCM secrets
google-services.json
**/google-services.json
*firebase-adminsdk*.json
GoogleService-Info.plist
!src/tests/
!src/tests/**

# WSL/Windows download metadata
*:Zone.Identifier

# curl cookie jar (contains session cookies - never commit)
/url
