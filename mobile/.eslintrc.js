// eslint-config-expo: Expo SDK 52 recommended ruleset (TS + RN + a11y).
module.exports = {
  root: true,
  extends: ['expo'],
  ignorePatterns: ['dist/', 'web-build/', 'android/', 'ios/', 'scripts/', 'node_modules/'],
};
